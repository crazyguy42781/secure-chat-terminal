[package]
name = "secure-chat-terminal"
version = "0.1.0"
edition = "2024"

[dependencies]
sequoia-openpgp = "1.12.0"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
chrono = "0.4"
uuid = { version = "1.3", features = ["v4"] }

